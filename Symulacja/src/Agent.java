abstract class Agent {

    int x,y;
    char sex;
    int power,speed;
     public Agent (int x, int y, char sex,int power,int speed){
         this.x=x;
         this.y=y;
         this.sex=sex;
         this.power=power;
         this.speed=speed;
     }
}

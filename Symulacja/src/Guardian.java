import java.util.Random;

public class Guardian extends Human {

   //int power;

    public Guardian( int x, int y, char sex, int power) {
        super(x,y,sex,power);
        //Random rand=new Random();
        //this.power=rand.nextInt(100);
    }
}
